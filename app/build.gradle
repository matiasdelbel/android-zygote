apply plugin: 'kotlin-android-extensions'

android.defaultConfig.applicationId = "com.mdelbel.android.features.template.testapp"

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
